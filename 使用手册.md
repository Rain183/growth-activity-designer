# growth-activity-designer · 使用手册

## 这是什么

一个把"超市用增活动怎么设计"的完整方法论装进 AI 的 skill：从一句业务诉求出发，按 S1-S8 分步推演，产出可评审的完整产品方案（活动规则/逆向/曝光动线/成本与实验/决策点汇报表）+ 一份可直接投屏的 HTML 方案报告。

## 目录导览

```
用户增长活动设计skill/
├── growth-activity-designer/   # skill 本体（可复用的知识与流程，不含任何一次运行的产物）
│   ├── SKILL.md            # 主控：S1-S8 路由表（先读这个）
│   ├── scripts/            # ledger 生产台账 + 六个门禁脚本 + 可视化渲染器
│   ├── references/         # 知识库：四步法/权益工具箱/逆向清单/触点清单/实验指引/
│   │                       # 信息量地图（动静分层）/玩法库七条目/案例/方案模板
│   └── test-scenarios.md   # 四个测试诉求
├── runs/                       # 运行实录（与 skill 平级：skill 是知识，run 是一次场景的产物）
│   ├── 消费挑战/            # 完整生产实录（T2）：S1-S8 全部产物 + ledger + 可视化 HTML
│   │                       #   + 11-实验判读与定档.md（上线后复盘记录，不属 skill 产物）
│   ├── 抽卡机盲测/          # 盲测实录（T1）：同上（验收用标准答案对照场景）
│   │                       #   注：本目录是 4 月盲测判分完成后的整理实录（对应 references/playbooks/
│   │                       #   p6 标注的"26年4月下旬入档"），不是盲测执行当时的产物；与
│   │                       #   test-scenarios.md 的 T1 考题登记合起来构成完整验收记录
│   ├── 沉默召回/            # 召回场景实录（T5）：门槛券包 + 定向触达、两级实验设计
│   └── 新客返红包/          # 逆向陷阱实录（T3）：S4 硬停否决原诉求后给出修正结构
└── mock数据/                   # 演示环境数据源：每类数据一张表，对应一个真实来源系统
    ├── README.md           # 数据源导览（先读）：S2 索取清单 → 对应 mock 表的取数路径
    └── …                   # BI大盘/客群分布/超市卡专题/历史活动先例/在投活动清单/
                            # 逆向水位/触点水位/财务口径/实验平台，共 9 张表
```

## 三步上手（仅需 Python 3.8+，Win/Mac 通用）

1. **看一次完整运行**：打开 `runs/消费挑战/10-方案可视化.html`（浏览器直接开，覆盖 S1–S7 产物），再对照同目录 `01-…` 到 `09-…` 逐份看每步产物；**上线后的判读与定档另见 `11-实验判读与定档.md`**（在 S8 之后产生，不参与渲染）。`进度视图.md` 是台账渲染的进度。
2. **体验门禁**：命令行进入 skill 目录（`growth-activity-designer/`），运行
   `python3 scripts/ledger.py status "../runs/消费挑战"`
   试试防跳步：新建一个空目录 init 后直接 advance S3，会被拒绝。
3. **跑一个新任务**：把 `test-scenarios.md` 里的 T3/T4 诉求（或自拟一句业务诉求）交给你的 AI 助手，让它按 `SKILL.md` 的路由推进——每步产物落 `../runs/{你的活动名}/`，用 `ledger.py advance` 过门禁。S2 索要当期数据时，把 `../mock数据/` 目录路径给它装载：索取清单里每条口径标注了来源系统，对应目录下的一张 mock 表（对照关系见 `mock数据/README.md`）；表里没有的数按"历史活动先例类比+标注假设+进实验位"处理（README 三条规则）。信息底座逐条标注来源与时点，全流程即可离线跑通——本资产包是演示复刻版，不连内部系统，动态数据一律走 mock 数据包。

## 常见问题

- 门禁报"产物缺失/要素未命中"：这是设计行为——按报错提示补齐产物再推进，不要绕过脚本改状态；
- 非消费型玩法（如访问型）没有商品池/金额口径：在规则产物里显式写"不适用：理由"，留白仍会被硬停；
- 改了已入账的产物：跑 `ledger.py diff` 会列出哈希不一致项，重跑对应步骤门禁重新入账。

## 数据口径声明

本资产包是**脱敏演示样本**：不连接内部系统，`mock数据/` 是按真实来源系统结构重建的模拟数据，其数值与项目交付的真实数字**不同**（真实口径以项目交付物为准）。本包用于完整跑通并展示流程，不是任何真实业务数据的副本。

因此，包内数字只保证**本沙盒内部自洽**（`mock数据/` 的先例锚点、`runs/` 各实录的判读结果、`references/` 的参数区间三处同源），与真实项目结果不作一致性要求。

